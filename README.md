# Related Rates Lighthouse Applet

Static GitHub Pages applet illustrating a lighthouse beam related-rates problem.

The app uses `tan(theta) = x/250`, the angular speed `dtheta/dt = pi/15 rad/s`, and computes `dx/dt = 250 sec^2(theta) dtheta/dt` in meters per second.
